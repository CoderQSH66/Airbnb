import styled from "styled-components"

const DemoWrapper = styled.div`
  .list {
    width: 200px;
    overflow: hidden;
    outline: 1px solid;
  }
  .item {
    padding: 20px;
  }
`

export default DemoWrapper
